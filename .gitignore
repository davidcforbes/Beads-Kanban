# SQLite databases
*.db
*.db?*
*.db-journal
*.db-wal
*.db-shm

# Daemon runtime files
daemon.lock
daemon.log
daemon.pid
bd.sock
sync-state.json
last-touched

# Local version tracking (prevents upgrade notification spam after git ops)
.local_version

# Local beads issue tracking (do not commit)
.beads/

# Legacy database files
db.sqlite
bd.db

# Worktree redirect file (contains relative path to main repo's .beads/)
# Must not be committed as paths would be wrong in other clones
redirect

# Merge artifacts (temporary files from 3-way merge)
beads.base.jsonl
beads.base.meta.json
beads.left.jsonl
beads.left.meta.json
beads.right.jsonl
beads.right.meta.json

# NOTE: Do NOT add negation patterns (e.g., !issues.jsonl) here.
# They would override the .beads ignore and allow local issue databases
# to be committed accidentally.

.Archive/
.vscode-test/
node_modules/
out/

# Build artifacts and packages
*.vsix
dist/

# Test reports and generated files
*-report.md
test-summary.md
benchmark-results/
test-databases/

# IDE and editor files
.serena/
*.code-workspace
.vscode/*.log

# Generated dependencies (copied by build script)
media/purify.min.js

# Archive directory for obsolete documentation
.Archive/

# Screenshots in root (organized versions are in images/screenshots/)
/Screenshot*.jpg
/Screenshot*.png
/*Edit Form*.jpg
/*Table*.jpg
/*Kanban*.jpg
